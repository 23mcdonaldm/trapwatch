
// Firebase dependencies are mocked for this environment to prevent import errors.
// In a real production setup, you would import from 'firebase/app', 'firebase/auth', etc.
// and configure with valid API keys.

export const auth = {};
export const googleProvider = {};
export const db = {};
